You'll need to install [nodejs](https://nodejs.org/en/) to complete the task. Make sure it's v8 or higher to run the ES6 code.

There are no dependencies to install. Simply run
```
    npm start
```
or
```
    node index.js
```

Check the logs for details